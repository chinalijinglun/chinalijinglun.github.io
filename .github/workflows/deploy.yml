# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Hexo

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["code"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Single deploy job since we're just deploying
  blog-jinglun:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '12.x'

      # - name: Cache node modules
      #   uses: actions/cache@v1
      #   with:
      #     path: ~/.npm
      #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      
      # - name: Install hexo dependencies
      #   run: |
      #     npm install -g hexo-cli
      #     npm install

      # - name: Generate files
      #   run: |
      #     hexo clean
      #     hexo generate

      - name: Build and Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          # PERSONAL_TOKEN: ${{ secrets.HEXO_TOKEN }}
          # PUBLISH_REPOSITORY: chinalijinglun/chinalijinglun.github.io # The repository the action should deploy to.
          # BRANCH: master  # The branch the action should deploy to.
          # PUBLISH_DIR: ./public # The folder the action should deploy.
          personal_token: ${{ secrets.HEXO_TOKEN }}
          publish_dir: ./public
          publish_branch: master
          external_repository: chinalijinglun/chinalijinglun.github.io
